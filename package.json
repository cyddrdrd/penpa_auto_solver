{
  "name": "penpa-spoiler",
  "version": "0.8.0",
  "private": true,
  "description": "Reconstruct the answer information stored in Penpa+ checker URLs",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}
